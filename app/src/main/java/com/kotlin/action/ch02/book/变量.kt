package com.kotlin.action.ch02.book.变量.kt

/**
 *createBy keepon
 */

//在Java 中声明变量的时候会以类型开始。在Kotlin 中这样是行不通的，因为许
//多变量声明的类型都可以省略。所以在Kotlin 中以关键字开始，然后是变量名称，
//最后可以加上类型（不加也可以） ：
//val question =
//”The Ultimate Question of Life, the Universe , and Everything ”
//val answer = 42
//这个例子省略了类型声明，但是如果需要也可以显式地指定变量的类型：
//val answer: Int = 42
//
//如果变量没有初始化器，需要显式地指定它的类型