package com.kotlin.action.ch02.book.目录和包

/**
 *createBy keepon
 */

//Kotlin 不区分导入的是类还是函数，而且，它允许使用import 关键字导入任
//何种类的声明。可以直接导入顶层函数的名称。
//
//package geometry.example
//import geometry.shapes.createRandomRectangle
//
//fun main(args: Array<String>) {
//    println(createRandomRectangle().isSquare)
//}

//也可以在包名称后加上．＊来导入特定包中定义的所有声明。注意这种星号导入
//不仅让包中定义的类可见， 也会让顶层函数和属性可见。

//在Java 中， 要把类放到和包结构相匹配的文件与目录结构中。例如，如
//果你有一个包含若干类的名为shapes 的包，必须把每一个类都放在一个有着
//和类相同名字的单独文件中，然后把这些文件放在一个名字为shapes 的目录
//中。

//在Kotlin 中，可以把多个类放在同一个文件中，文件的名字还可以随意选择。
//Ko ti in 也没有对磁盘上源文件的布局强加任何限制。比如，可以把包geometry.
//sha pes 所有的内容都放在文件s hapes.kt 中，并把这个文件直接放在目录geome町F
//中，而不需要再创建一个独立的shapes 文件夹
































